[project]
name = "bettafish-backend"
version = "0.1.0"
description = "舆情收集后端 - 基于 NewsNow API 聚合多平台热点"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "httpx>=0.27.0",
    "pydantic>=2.9.0",
]

[tool.uv]
package = false
